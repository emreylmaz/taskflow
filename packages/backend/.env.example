# Database
DATABASE_URL="postgresql://taskflow:YOUR_DB_PASSWORD@localhost:5432/taskflow?schema=public"

# Server
PORT=4000
NODE_ENV=development

# JWT Secrets (generate with: openssl rand -base64 32)
JWT_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
